# MVP 구현 결정과 SPEC 정합성

## 2026-09-18 Room Code 6자리 숫자화

사용자 요청에 따라 Room Code 계약을 정확히 6자리 숫자 문자열로 확정했다.
서버는 `000000`~`999999` 범위에서 코드를 생성하고 앞자리 `0`을 보존하며,
현재 존재하는 Room과 충돌하면 다시 생성한다. 생성 응답, 코드 조회와
`/state`는 숫자로 변환하지 않고 문자열을 유지한다.

## 2026-09-15 공통 문서 동기화

사용자 요청에 따라 `noopi-web/docs/common/`을 기준으로 이 프로젝트의
`docs/common/` 파일 구성과 내용을 동일하게 맞췄다. 윷놀이뿐 아니라
마피아 명세와 기존 서비스/API/Realtime/라이어 문서 차이도 함께 반영했다.
공통 문서의 의미를 이 프로젝트에서 별도로 수정하지 않는다.

기존 Backend에만 있던 운영 통계 계약은 `OPERATIONS_API.md`로 이동하여
보존했다. 운영 API를 삭제하거나 동작을 변경한 것이 아니다.

최초 작업은 문서 정비였으며, 이후 사용자 요청으로 `YUT` 실제 구현을 추가했다.
현재 `GameApplication`의 생성/시작/취소/상태 조회와 `YutController`의 5개 행동
API가 `game/yut` Runtime·경로·서비스·Projection에 연결되어 있다.
기존 라이어/블라인드/마피아 구현은 유지했다.

## 피그 구현/검증 체크리스트

- [x] 카탈로그 및 `gameType = PIG`, 빈 config, 2~6명 검증.
- [x] `game/pig` Runtime·Service·Projection을 다른 게임과 독립 구성.
- [x] 첫 던지기 20%, 성공마다 10%p 증가, 최대 90%인 서버 확률 판정으로 변경.
- [x] `1`이 아니면 `2`~`6`을 동일 확률로 판정하고 같은 숫자의 반복을 허용.
- [x] 숫자 제거 상태를 없애고 `successfulRollCount`와 서버 확정 `bustProbability`를 Projection에 제공.
- [x] 1 발생 시 턴 점수 소멸, 총점 유지, 다음 PLAYING Player 전환.
- [x] STOP 점수 확정과 50점 도달 순서 기반 FINISHED/순위 처리.
- [x] FINISHED Player 턴 제외와 마지막 Player 자동 마지막 순위.
- [x] ROLL/STOP `Idempotency-Key` 중복 방지와 Room 단위 원자 처리.
- [x] 현재 Player에게만 `allowedActions`를 제공하는 `/state` Projection.
- [x] PIG 전용 이벤트와 공통 `GAME_FINISHED` 연동.
- [x] 규칙 단위 테스트와 HTTP/WebSocket 통합 테스트 추가.

PIG는 Runtime 전용 상태이므로 DB/Flyway 변경이 없다. 윷놀이 결과 UI와의
디자인 유사성은 Frontend 책임이며 Backend 게임 모듈 간 의존은 없다.

2026-09-20에 PIG의 숫자 제거 규칙을 폐기하고 성공 횟수 기반 위험도 증가
규칙으로 공통 SPEC과 API 계약을 변경했으며 Backend 구현과 테스트에도
동일한 계약을 반영했다.

## 윷놀이 구현/검증 체크리스트

아래는 구현 및 테스트 확인 목록이다. 규칙은
`common/games/YUT_GAME_SPEC.md`, DTO/상태/오류/이벤트는 `common/API_SPEC.md`를 따른다.

- [x] 카탈로그 및 `gameType = YUT`, `config.mode = INDIVIDUAL | TEAM` 지원.
- [x] 개인전 2~4명 및 Player별 4개 말, 팀전 4명 및 팀별 공유 말 4개 검증.
- [x] 팀 선택/변경 정원 2명 원자적 검증, 만원 팀 변경 실패 시 기존 소속 유지.
- [x] 방장 시작 권한, 팀 배정 완료 조건, 시작 시 참가자 snapshot과 교차 턴 순서.
- [x] 서버 5% 확률 낙 판정. 해당 던지기만 무효로 하고 기존 이동권은 유지하며,
  기존 이동권이 없을 때만 즉시 턴 전환.
- [x] 낙이 아닐 때 특수 윷가락의 누피 앞면을 포함한 4개 윷가락 조합에 따른
  빽도/도/개/걸/윷/모 판정.
- [x] 빽도의 실제 진입 경로 역행, `OUTER_1 → OUTER_20`,
  `OUTER_20 → FINISHED`, 도착 칸 업기/잡기 판정.
- [x] 윷/모 연속 추가 던지기 → 이동권 선택/소비 → 잡기 추가 던지기 순서.
- [x] 여러 이동권을 원하는 순서로 소비하며 같은 말 또는 서로 다른 말 선택.
- [x] Node/Edge 경로, 갈림길 정확한 도착과 통과 구분, 서버 경로 후보 제공.
- [x] 같은 소유자 그룹 업기/공동 이동, 상대 그룹 전체 READY 복귀.
- [x] 상대 칸을 지나갈 때 잡지 않으며 정확한 도착 시에만 잡기 보너스 1회.
- [x] 도착점 도달과 통과 구분, 업힌 그룹 전체 완주, 완주한 말 선택 불가.
- [x] 개인전 말 4개 완주 시 순위 확정, 남은 행동 소멸, 턴 제외 및 관전 전환.
- [x] 개인전은 마지막 한 명만 남으면 꼴등을 자동 확정해 전체 순위로 종료하고,
  팀전은 한 팀 완주 시 즉시 승리.
- [x] 현재 Player의 `myAction` 및 후보 검증, 다른 Player/완주 관전자 행동 거부.
- [x] 팀 변경 204, 던지기 200, 이동권 선택 204, 말 선택 200 또는 경로 대기 202,
  경로 선택 200 및 공통 이동 결과 필드 검증.
- [x] 동일 이동권 이중 소비, 중복 이동/잡기 보너스, 종료 후 행동 방지.
- [x] `YUT_*` 이벤트의 확정 결과와 `/state` 일치, 후보 목록 broadcast 금지.
- [x] 재접속/새로고침 후 선택 중인 말/이동권/경로, 추가 던지기와 그룹 복구.
- [x] 윷/모 연속, 잡기 후 남은 이동권, 지름길, 업힌 그룹 잡기/완주를 포함한 테스트.

### 프론트 연출 및 Mock과의 경계

길게 누르기와 파워 게이지는 Client의 던지기 연출이다. 현재 던지기 요청은
body가 없으며 `power` 같은 필드를 임의 추가하지 않는다. 한 칸씩 이동,
상대 말 밀쳐내기, `x2` 표시는 서버가 확정한 말 위치/그룹/잡기 결과의 표현이다.
애니메이션 중간 위치를 실제 게임 상태로 저장하거나 애니메이션 완료 API를 만들지 않는다.
낙의 바깥 튕김 연출은 `lastThrow.sequence`와 서버의 `NAK` 결과만 사용하며,
Client가 낙이나 턴 전환을 자체 판정하지 않는다.

Mock의 고정 `GAE`, 완주 직전 말 배치 및 단순 외곽 경로는 확인용 시나리오다.
실제 서버의 난수, 지름길 및 도착점 통과 판정을 Mock 코드에서 복사하지 않는다.

### 확정한 보드 계약과 남은 재전송 제약

사용자 승인으로 전체 Node/Path ID와 도착점 통과 기준을 공통 윷놀이 명세
9.1절에 확정했다. 외곽 20칸, 중앙을 공유하는 대각선 9칸과 네 경로 ID를
서버 `YutBoard` 및 프론트 표시 좌표에 반영했다. 중앙 갈림길과 업힐 때 경로
기준도 같은 절을 따른다. 이동 경유 노드 DTO는 추가하지 않았다.

프론트는 서버 확정 시작/도착 Node 사이의 표시용 경로만 한 칸씩 연출한다.
유효 후보, 실제 이동 위치와 승패는 서버에서만 계산한다.

현재 SPEC: 던지기는 body 없는 POST이며 중복 행동 오류가 정의되어 있다.

충돌/누락: 연속 추가 던지기가 가능한 동일 Player의 재전송 요청과 의도된
다음 던지기를 구분하는 요청 식별 계약은 명시되어 있지 않다.

사용자 승인: 기존 body 없는 계약과 서버 동시성/단계 검증을 우선 유지한다.
Client는 던지기를 자동 재시도하지 않으며 응답 유실 시 `/state`를 먼저 조회한다.
요청 ID나 헤더를 필수 계약으로 추가하지 않는다. 완전한 지연 재전송 식별은
별도 계약 변경이 필요한 잔여 제약이다.

영향 범위: 던지기 멱등 처리, 네트워크 재시도 및 추가 던지기 테스트.

### 공통 동기화로 확인된 기존 차이

실제 HTTP/WebSocket 테스트에서 `Map.copyOf(payload)`가 `null` 출발/도착
Node를 거부하여 이동 이벤트를 누락시키는 문제를 확인했다. null을 허용하는
방어적 Map 복사로 수정했으며 출발·잡기·완주 이벤트를 실제 소켓으로 검증했다.
프론트 HTTP 어댑터도 body가 없는 `202 Accepted`를 정상 처리하도록 수정했다.

프론트 공통 API/Realtime은 방장 미접속 중에도 참가 가능한 Room의 조회/참가를
허용한다. 최종 확인한 `GameApplication.lookup/join`에도 방장 연결을 이유로
신규 참가를 막는 검사는 없다. 이번 문서 작업에서 코드를 바꾸지는 않았다.

`SERVICE_SPEC.md`에는 방장 명시적 이탈 시 Room 종료 원칙과 영구 이탈 시
권한 이전 가능 문장이 함께 남아 있다. API/Realtime의 명시적 나가기 계약은
`HOST_LEFT` 종료다. 별도의 영구 이탈/승계 정책은 임의 구현하지 않고 확정한다.

## 마피아 게임 명세 반영

공통 `MAFIA_GAME_SPEC.md`와 API/Realtime 계약을 Backend 문서에 반영했다.

마피아 상태는 `MafiaGameRuntime`이 소유하며 역할·생존 상태, 밤 행동,
경찰 조사 기록, 시민 의심 기록·집계, 처형·재투표·찬반 투표와 승패를
GameSession Memory에서 관리한다. 별도 영구 콘텐츠가 없으므로 마피아 전용 DB
테이블을 추가하지 않는다.

역할, 마피아 동료, 경찰 조사 결과, 시민별 의심 대상·개인 의심 수는
요청 Player 기준 Projection으로 보호한다. WebSocket은 공개 가능한 phase·완료
상태만 알리고 Client는 `/state`로 복구한다. 처형 투표의 개인별 대상과 찬반
투표의 개인별 선택은 종료 후에도 공개하지 않는다.

공통 SPEC에서 마피아 장기 미접속 Player 제외 정책은 V1 범위로 정의하지
않았다. 라이어 게임의 exclude/자동 취소 정책을 마피아에 임의로 적용하지 않는다.

현재 SPEC: `MAFIA_GAME_SPEC.md` 18.1절·30절은 `VOTE_RESULT → EXECUTION`을
기술하고 `JUDGMENT`/`JUDGMENT_RESULT`를 phase 목록에 포함하지 않는다.

충돌/누락: `API_SPEC.md` 36.8절의 최후의 변론·찬반 투표 계약은
`VOTE_RESULT → JUDGMENT → JUDGMENT_RESULT → EXECUTION 또는 NIGHT`를 사용한다.

제안: 다음 공통 SPEC 정리 시 `MAFIA_GAME_SPEC.md`의 phase 목록·전체 흐름을
API 계약과 같은 `JUDGMENT`/`JUDGMENT_RESULT` 단계로 정정한다.

영향 범위: phase enum, 방장 단계 진행, 찬반 투표 제출, `/state`,
`MAFIA_PHASE_CHANGED` 이벤트. Backend 문서와 현재 구현은 API 계약의 phase를 기준으로 기록한다.

## 블라인드 게임 명세 추가

공통 `BLIND_GAME_SPEC.md`와 API/Realtime 계약에 `BLIND`를 추가했다.

Backend는 기존 Room, GameSession, `/state`, WebSocket 갱신 구조를 재사용하고
제시어는 카테고리 없는 블라인드 전용 Repository에서 조회한다. 블라인드 전용 Runtime은 Player별 제시어
배정, `GUESSING` phase와 승자만 관리하며 질문·턴·타이머 상태는 만들지
않는다.

동시 정답은 Room 단위 동시성 경계에서 한 명만 승자로 확정한다. 명시적
퇴장 또는 장기 미접속 시 승패/취소 정책은 아직 확정되지 않았으므로 임의
구현하지 않는다.

## 확정 규칙 정정

현재 SPEC: LIAR_GAME_SPEC 18절/29절 일부 문구가 개인 투표 공개·최대 재투표 2회·랜덤 지목을 기술했다.

충돌/누락: AGENTS.md 및 최신 API/Realtime 계약과 충돌했다.

제안 및 반영: 사용자 지시대로 개별 투표는 항상 비공개, 무제한 동률 후보 재투표로 정정했다.
REALTIME_SPEC의 ‘실제 투표 내용’ 문구도 후보별 최종 득표수로 명확히 했다.

영향 범위: 라이어 규칙, 상태 projection, 모든 투표 이벤트, 100회 연속 동률 테스트.

## 종료 결과 제시어

현재 SPEC: 라이어 keyword는 항상 null이라는 일반 규칙과 API_SPEC 22절 전체 참가자에게 최종 keyword 공개가 충돌했다.

충돌/누락: FINISHED의 공개 범위가 상충했다.

제안 및 사용자 확정: 게임 진행 중 비공개, 종료 결과에서는 라이어에게도 공개한다.

영향 범위: FINISHED의 result.keyword, AGENTS.md 보완, 공통 게임 문서, 최종 결과 테스트.

## 제외된 Player가 받은 표

현재 SPEC: 제외된 Player가 제출한 표를 무효화하고 필요한 투표 수를 다시 계산한다.

충돌/누락: 다른 Player가 제외 대상에게 제출한 표와 후보 자격 처리가 빠져 있었다.

사용자 확정: 이미 받은 표는 유지하고 해당 라운드 후보 자격을 유지한다.

영향 범위: 제외 후 집계, eligibleCandidates, 투표 완료 여부. 이후 동률은 기존 동률 후보 규칙을 따른다.

## 명시적 Room 종료 API

현재 SPEC: SERVICE_SPEC에 방장의 Room 종료 권한이 있으나 API_SPEC 34절에 종료 endpoint가 없다.

충돌/누락: method/path, 응답, 오류 및 이벤트 계약이 없다.

제안: 별도 계약 확정 후 추가한다. 임의 API를 추가하지 않았다.
현재는 방장의 명시적 나가기(`HOST_LEFT`) 및 설정된 TTL 만료 시 Room과 연결 참조를 정리한다.

영향 범위: 방장 전용 Room 종료 버튼을 연동하려면 계약 확정이 필요하다.

## 제외 전용 실시간 알림

현재 SPEC: exclude API는 204를 반환하며 제외 이벤트는 API/REALTIME_SPEC의 이벤트 목록에 없다.

충돌/누락: 제외만 발생하고 아직 집계되지 않을 때 다른 Client에 즉시 알릴 이벤트가 없다.

제안: 새 이벤트가 필요하면 이름/payload를 계약에 추가한다. 임의 이벤트는 추가하지 않았다.
현재는 요청 완료 후 /state로 복구하며 집계 또는 취소 시 정의된 게임 이벤트를 전달한다.

영향 범위: 제외 이후 다른 Client의 즉시 진행 인원 갱신. 재접속 및 상태 재조회는 정상 복구한다.

## 운영 구현 세부사항

- 명시적 방장 탈퇴: `ROOM_CLOSED`를 `HOST_LEFT` 사유로 발행하고 Room 종료.
- 방장 미접속: 자동 승계/종료하지 않으며 재접속을 기다린다.
- 운영 기본값: disconnect grace 2분, Room TTL 12시간, cleanup 60초, 최근 제시어 10개.
- 값은 환경 변수로 변경한다. 별도의 gameplay 제한이나 재투표 제한이 아니다.
- VOTE_RESULT와 LIAR_REVEAL은 표준 이벤트를 발행하는 자동 전환 단계다.
- 본인 투표 금지로 더 진행할 수 없는 극단적 중도 이탈 상황에 대한 새 승패 규칙은 추가하지 않는다.
  방장은 기존 cancel API로 취소할 수 있다.
- 실제 라이어 검거 후 `LIAR_GUESS` 상태에는 공개된 `liarPlayer`를 모든 참가자에게 제공한다.
  시민 대기 화면은 이 값을 사용하며, 제시어와 투표 대상의 공개 범위는 바뀌지 않는다.
- `READY` 상태에는 GameSession 생성 시 확정한 `categoryCode`, `categoryName`을 제공한다.
  방장이 아닌 참가자와 새로고침한 사용자도 준비 화면을 `/state`만으로 복구한다.
